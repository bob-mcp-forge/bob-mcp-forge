# Frontend

React + Tailwind app. Owner: **Tien**.

See `/docs/DAY1_QUICKSTART.md` for setup instructions.

```bash
# Setup (Day 1)
npm create vite@latest . -- --template react-ts
npm install
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```
